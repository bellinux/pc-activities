ACTIVIDAD: 21.1-cinematic-power-on
CODE: luz-encendida-ruido-animacion
COMPONENTES: NivelRuido1=NoiseLevel, DibujoLED2=MatrixDraw

protobject_NivelRuido1_change  property=suddenIncrease
  ESTABLECER var:estado = no(var:estado)
  SI
    [si var:estado]
    CONTAR CON columnas DE 1 A 7 PASO 1
        ; // Este bloque crea una variable llamada "columnas". Luego asigna valores desde 1 hasta 7 en aumentos de 1 y cada vez que toma un nuevo valor se ejecutará lo de dentro.  Es decir inicia con columnas = 1, luego ejecuta lo de dentro y al terminar ahora columnas = 2 y se vuelve a ejecutar todo. Una vez columnas = 7 se ejecuta lo de dentro y pasa al siguiente bloque de instrucciones que no esté dentro, en este caso pasa al bloque "dibuja en x..."
      CONTAR CON lineas DE 1 A 7 PASO 1
          ; // Este bloque crea una variable llamada "lineas". Luego asigna valores desde 1 hasta 7 en aumentos de 1 y cada vez que toma un nuevo valor se ejecutará lo de dentro.  Es decir inicia con lineas = 1, luego ejecuta lo de dentro y al terminar ahora lineas = 2 y se vuelve a ejecutar todo. Una vez lineas = 7 se ejecuta lo de dentro y pasa al siguiente bloque de instrucciones que no esté dentro, en este caso pasa al bloque dibuja en x...  Se puede analizar que por cada vez que el valor de "columnas" cambia, se ejecuta 7 veces el bloque donde cambia fila y como columnas cambia 7 veces, el bloque de "dibuja en x..." se ejecuta 7x7 = 49 veces, que corresponde a la cantidad total de leds.
        DibujoLED2_drawOnXY_set_2  VAR=var:lineas  VAR2=var:columnas  VAR3=colour_picker(COLOUR=#ff4444)
        ESPERAR  ms  VAR=10
    DibujoLED2_drawOnXY_set_2  VAR=1  VAR2=1  VAR3=colour_picker(COLOUR=#ff4444)
    [sino]
    CONTAR CON columnas DE 7 A 1 PASO 1
        ; // Este bloque crea una variable llamada "columnas". Luego asigna valores desde 1 hasta 7 en aumentos de 1 y cada vez que toma un nuevo valor se ejecutará lo de dentro.  Es decir inicia con columnas = 1, luego ejecuta lo de dentro y al terminar ahora columnas = 2 y se vuelve a ejecutar todo. Una vez columnas = 7 se ejecuta lo de dentro y pasa al siguiente bloque de instrucciones que no esté dentro, en este caso pasa al bloque "dibuja en x..."
      CONTAR CON lineas DE 7 A 1 PASO 1
          ; // Este bloque crea una variable llamada "lineas". Luego asigna valores desde 1 hasta 7 en aumentos de 1 y cada vez que toma un nuevo valor se ejecutará lo de dentro.  Es decir inicia con lineas = 1, luego ejecuta lo de dentro y al terminar ahora lineas = 2 y se vuelve a ejecutar todo. Una vez lineas = 7 se ejecuta lo de dentro y pasa al siguiente bloque de instrucciones que no esté dentro, en este caso pasa al bloque dibuja en x...  Se puede analizar que por cada vez que el valor de "columnas" cambia, se ejecuta 7 veces el bloque donde cambia fila y como columnas cambia 7 veces, el bloque de "dibuja en x..." se ejecuta 7x7 = 49 veces, que corresponde a la cantidad total de leds.
        DibujoLED2_drawOnXY_set_2  VAR=var:lineas  VAR2=var:columnas  VAR3=colour_picker(COLOUR=#000000)
        ESPERAR  ms  VAR=10

AL INICIAR
  ESTABLECER var:estado = FALSE
  DibujoLED2_background_set_3  VAR=colour_picker(COLOUR=#000000)
  DibujoLED2_clear_set_4
