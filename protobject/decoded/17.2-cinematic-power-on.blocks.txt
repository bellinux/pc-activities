ACTIVIDAD: 17.2-cinematic-power-on
FUENTE: Protobject (bloques, etiquetas oficiales es)

cuando NivelRuido1 detecta aumento repentino:
  establecer estado a no (estado)
  si estado:
    contar con filas desde 1 hasta 7 de a 1:   // Este 'contar con' da a 'filas' los valores del 1 al 7 y ejecuta lo de dentro por cada valor, recorriendo las filas una a una.
      contar con columnas desde 1 hasta 7 de a 1:   // Este 'contar con' da a 'columnas' los valores del 1 al 7 dentro de cada fila, así que dibuja 7x7 = 49 veces: una por cada led.
        dibuja en x columnas y filas con color #ff4444 en DibujoLED2
        retraso de 10 milisegundos
  sino:
    contar con filas desde 1 hasta 7 de a 1:   // Este 'contar con' recorre las filas del 1 al 7 para apagar la luz de cada una.
      contar con columnas desde 1 hasta 7 de a 1:   // Dentro de cada fila, este 'contar con' da a 'columnas' los valores del 1 al 7 y apaga el led de cada columna.
        dibuja en x columnas y filas con color #000000 en DibujoLED2
        retraso de 10 milisegundos

al iniciar el programa:
  establecer estado a falso
