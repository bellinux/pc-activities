# ⚠️ NUNCA PUBLICAR — material reservado (papers, work-in-progress, borradores).
# Regla dura: jamás se versiona ni se sube, ni siquiera al pedir "pushear todo". Ver CLAUDE.md §2.
context-not-to-publish/

# Configuración local de Claude Code / IDE (no publicar)
.claude/

# Dependencias y artefactos de Cloudflare Workers / wrangler
node_modules/
.wrangler/

# Backups locales de .ptj (no publicar)
protobject/backup/

# Cachés de Python
__pycache__/
*.pyc

# Temporales del SO
.DS_Store
Thumbs.db
desktop.ini

# Metadatos de Windows (marca "descargado de Internet"). Llevan ":" en el nombre,
# que Windows no admite como archivo: si se versionan, rompen el checkout y el index.
*:Zone.Identifier
